import React from 'react'
import {useSelector} from 'react-redux'
const UserProfile = () => {
    const myUser = useSelector(state => state.state)
    return (
        <div>
            
        </div>
    )
}

export default UserProfile
